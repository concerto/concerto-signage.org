<div class="content">
  <p id="notice"><%= notice %></p>

  <h1><%= @page.title %></h1>

  <p>
    <%=raw @page.body %>
  </p>

  <% if current_user %>
  <%= link_to 'Edit', edit_page_path(@page) %>
  <% end %>
</div>
